<div class="regist-form">
<%= form_for(@users) do |f| %>
  <% if @users.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@users.errors.count, "error") %> prohibited this login from being saved:</h2>

      <ul>
      <% @users.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="f-label float-left"> <%= f.label :email %></div>
    <div class="f-field float-left"><%= f.text_field :email %></div>
  </div>
  <div class="field">
    <div class="f-label float-left"> <%= f.label :username %></div>
     <div class="f-field float-left"><%= f.text_field :username %></div>
   </div>

  <div class="field">
     <div class="f-label float-left"> <%= f.label :password %></div>
     <div class="f-field float-left"><%= f.password_field :password %></div>
   </div>

  <div class="actions">
    <%= f.submit "注册"%>
  </div>
<% end %>
  </div>
